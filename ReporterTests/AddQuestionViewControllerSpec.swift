//
//  ReporterTests.swift
//  ReporterTests
//
//  Created by Veronica Ray on 2/20/15.
//  Copyright (c) 2015 Veronica Ray. All rights reserved.
//

import UIKit
import Quick
import Nimble
import ReporterProject

class AddQuestionViewControllerSpec: QuickSpec  {
    override func setUp() {
        // When a failure occurs for an example, throw an exception
        // to bail out of execution instead of continuing to run.
        //
        // Setting this to false makes this group of tests behave more like
        // other testing frameworks (jUnit, rspec, etc.)
        continueAfterFailure = false
    }
    
    override func spec() {
//                expect(AddQuestionViewController().navigationItem.title).to(equal("something"))

     expect("nothing").to(equal("something"))}
}